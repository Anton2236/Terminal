<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.axotsoft.blurminal2">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service
            android:name=".widget.CommandWidgetService"
            android:exported="false" />

        <receiver
            android:name=".devices.DeviceNameChangedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.NAME_CHANGED" />
            </intent-filter>
        </receiver>
        <receiver android:name=".widget.BluetoothCommandWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/bluetooth_command_widget_info" />
        </receiver>

        <activity android:name=".widget.BluetoothCommandWidgetConfigureActivity">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.DeviceChooserActivity" />

        <provider
            android:name=".provider.BluetoothDevicesProvider"
            android:authorities="com.axotsoft.blurminal2.authority"
            android:enabled="true"
            android:exported="false" />

        <service
            android:name=".bluetooth.BluetoothConnectionService"
            android:enabled="true"
            android:exported="false" /> <!-- android:process=":BluetoothConnection" /> -->
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>